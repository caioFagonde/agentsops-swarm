[project]
name = "agentops-swarm"
version = "3.0.0"
description = "Local multi-agent worktree orchestration for Claude Code, Codex, and Antigravity"
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = "-q"
